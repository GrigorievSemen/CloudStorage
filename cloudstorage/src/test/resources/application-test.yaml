spring:

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL82Dialect

  datasource:
    url: jdbc:postgresql://localhost:5431/cloud_storage
    username: postgres
    password: postgres

#  sql:
#    init:
#      mode: always
#      data-locations: classpath:db/initDB.sql,classpath:db/populateDB.sql

server:
  port: 8090

jwt:
  token:
    secret: jwtappdemo
    expired: 36000000
